# 数据库（本地开发用SQLite，生产可改为MySQL）
DATABASE_URL=sqlite+aiosqlite:///./data/weekly_report.db

# JWT密钥（生产环境请更换）
SECRET_KEY=your-secret-key-change-in-production

# 管理员初始密码（首次启动时创建，之后可通过系统修改）
ADMIN_PASSWORD=change-this-to-a-complex-password

# 应用配置
DEBUG=true
